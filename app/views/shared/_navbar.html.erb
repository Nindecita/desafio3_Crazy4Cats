<nav class="navbar navbar-expand-lg navbar">
  <div class="container-fluid">
    <a class="navbar-brand navbar" href="/">Crazy 4 Cats</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link active navbar" aria-current="page" href="/aventures">Aventuras</a>
          </li>
        <%end%>
        <li class="nav-item">
          <li class="nav-item dropdown">
            <a class="nav-link navbar dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
              <% if user_signed_in? %>
                <%= current_user.email%>
              <%else%>
                Iniciar sesión  |  Registrarse
              <%end%>
            </a>
            <ul class="dropdown-menu navbar">
              <% if user_signed_in? %>
                <li>
                  <%= button_to 'Cerrar sesión' , destroy_user_session_path, class: 'dropdown-item navbar' , method:
                      :delete %>
                </li>
              <%else%>
                <li>
                  <%= link_to 'Iniciar sesión' , new_user_session_path, class: 'dropdown-item navbar' %>
                </li>
                <li>
                  <hr class="dropdown-divider">
                </li>
                <li>
                  <%= link_to 'Registrarse' , new_user_registration_path, class: 'dropdown-item navbar' %>
                </li>
              <%end%>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
